const { TextRun, h1, h2, h3, p, pc, bulletItem, numberedItem, makeTable, figureImage } = require("./build.js");

const chapterThree = [
  h1("CHAPTER THREE: SYSTEM ANALYSIS AND DESIGN"),

  h2("3.1 Research Methodology"),
  p("This project adopts the Cross-Industry Standard Process for Data Mining (CRISP-DM) as its overarching methodology, consistent with its use in comparable Nigerian EDM studies reviewed in Chapter Two (Ekubo & Esiefarienrhe, 2022; Jibril, 2026). CRISP-DM structures the work into six iterative phases: business understanding, data understanding, data preparation, modelling, evaluation, and deployment. The business-understanding and data-understanding phases are addressed by Chapters One and Two and Sections 3.2-3.4 below; data preparation, modelling, and evaluation are addressed in Sections 3.5-3.9 and demonstrated in Chapter Four; and deployment is addressed conceptually in Section 3.10 and Chapter Five, since an actual institutional deployment is outside the scope of a course project."),

  h2("3.2 Analysis of the Existing System"),
  p("The existing approach to monitoring student performance at the University of Ibadan, as at most Nigerian public universities, is manual and retrospective. Continuous assessment and examination scores are recorded by individual departments and consolidated into semester results, which are then reviewed by departmental and faculty boards, typically at the end of each semester. Students whose CGPA falls below defined thresholds are placed on probation or, in severe cases, are withdrawn, in accordance with the university's academic regulations. This existing process has several limitations relevant to the design of the proposed system:"),
  bulletItem("It is reactive rather than predictive: intervention decisions are made only after a semester's results are finalised, by which point the affected courses cannot be retaken within the same academic session without incurring a delay."),
  bulletItem("It relies on a single summary indicator (CGPA) rather than on the richer set of behavioural and contextual data (attendance, study patterns, course load) that is often already collected in fragmented form across departmental and administrative systems but rarely integrated or analysed jointly."),
  bulletItem("It does not scale efficiently: with large cohorts, manual review by departmental and faculty boards cannot easily prioritise attention toward the specific students who would most benefit from early intervention."),
  bulletItem("It provides no forward-looking estimate of risk during an ongoing semester, when intervention would be most useful."),
  p("These limitations, rather than any absence of relevant institutional data, are the primary justification for the machine learning-based system proposed in this chapter: the raw data needed already exists in various university systems, but no analytical layer currently synthesises it into an early, actionable risk signal."),

  h2("3.3 Requirements of the Proposed System"),
  h3("3.3.1 Functional Requirements"),
  numberedItem("The system shall ingest structured student records comprising admission, demographic, attendance, and academic-history attributes.", "func-req"),
  numberedItem("The system shall preprocess ingested data, including handling of missing values, encoding of categorical variables, normalisation of numeric variables, and balancing of class distributions.", "func-req"),
  numberedItem("The system shall support feature selection to identify the subset of attributes most predictive of academic performance.", "func-req"),
  numberedItem("The system shall train and validate multiple classification algorithms and select the best-performing model according to a defined set of evaluation metrics.", "func-req"),
  numberedItem("The system shall classify individual students into performance-risk categories (e.g., At-Risk, Average, High-Performing) and produce a corresponding confidence score.", "func-req"),
  numberedItem("The system shall generate interpretable explanations (e.g., SHAP-based feature attributions) for each prediction, to support use by non-technical academic staff.", "func-req"),
  numberedItem("The system shall present outputs through a decision-support interface accessible to authorised academic advisers and departmental staff.", "func-req"),

  h3("3.3.2 Non-Functional Requirements"),
  bulletItem("Interpretability: model outputs must be explainable in terms that academic advisers, who are not data scientists, can understand and act upon."),
  bulletItem("Fairness: the system must be periodically audited for disparities in predictive performance across demographic and socioeconomic subgroups (e.g., gender, income level, state of origin), consistent with the concerns raised by Kizilcec and Lee (2020) and empirically observed by Mamudu et al. (2026)."),
  bulletItem("Privacy and data protection: all student data must be processed in compliance with the Nigeria Data Protection Act 2023 and applicable University of Ibadan data governance policies, with access restricted to authorised personnel and predictions used only to support, not replace, human decision-making."),
  bulletItem("Computational feasibility: the system should be trainable and operable on standard institutional computing hardware without requiring specialised GPU infrastructure, given the resource constraints typical of Nigerian public universities (Mamudu et al., 2026)."),
  bulletItem("Maintainability: the modelling pipeline should support periodic retraining as new cohort data becomes available, to guard against model drift."),

  h2("3.4 Dataset and Relevant Student Attributes"),
  p("Because access to real, individually identifiable University of Ibadan student records was not available for this course project (Section 1.6), this section specifies the dataset and attributes that the proposed system is designed to consume, drawing on the attribute sets reported as most predictive across the literature reviewed in Chapter Two (Adekitan & Salau, 2020; Mamudu et al., 2026). Chapter Four then demonstrates the pipeline using a synthetic dataset constructed to reflect this same attribute structure and plausible statistical relationships."),
  p("Table 3.1 specifies the proposed attribute set, grouped into four categories: pre-admission, academic, behavioural, and socio-demographic."),
  makeTable(
    ["Category", "Attribute", "Description / Type"],
    [
      ["Pre-admission", "UTME/JAMB score", "Numeric (typically 0-400); admission examination score"],
      ["Pre-admission", "O'level credit count", "Numeric; number of credit passes in relevant subjects (WAEC/NECO)"],
      ["Academic", "Previous semester GPA", "Numeric (0.00-5.00); most recent semester's grade point average"],
      ["Academic", "Registered credit load", "Numeric; number of course units registered for the semester"],
      ["Behavioural", "Attendance rate", "Numeric (%); proportion of lectures/tutorials attended"],
      ["Behavioural", "Average daily self-study hours", "Numeric; self-reported or LMS-derived study time"],
      ["Behavioural", "Extracurricular engagement hours", "Numeric; time spent in clubs, sports, or other non-academic activities"],
      ["Socio-demographic", "Family income level", "Categorical (low/medium/high), self-reported at admission"],
      ["Socio-demographic", "Hostel residence status", "Binary; whether the student resides on-campus"],
      ["Socio-demographic", "Internet access", "Binary; regular access to internet for study purposes"],
      ["Target", "Performance class", "Categorical (At-Risk / Average / High-Performing), derived from projected or actual next-semester CGPA band"],
    ],
    [1800, 2600, 4800]
  ),
  p("This attribute set deliberately excludes ethnicity and geopolitical zone of origin as predictive features, in direct response to the finding by Adekitan and Salau (2020) that such variables are not statistically significant predictors of academic outcomes once genuine academic and behavioural variables are controlled for, and out of caution regarding the risk of such proxies encoding unfair bias rather than genuine predictive signal.", { indent: false }),

  h2("3.5 Data Preprocessing"),
  p("The proposed preprocessing pipeline follows standard EDM practice as applied in the reviewed Nigerian studies (Ekubo & Esiefarienrhe, 2022; Mamudu et al., 2026) and comprises the following steps, applied in sequence:"),
  numberedItem("Data cleaning: removal of duplicate records and correction or removal of implausible values (e.g., attendance above 100 percent)."),
  numberedItem("Missing-value handling: numeric attributes with missing values are imputed using median values within the relevant academic department or cohort; categorical attributes are imputed using the most frequent category, with an explicit \"unknown\" category retained where missingness itself may be informative."),
  numberedItem("Categorical encoding: nominal categorical variables (e.g., family income level, hostel residence) are converted to numeric form using one-hot or ordinal encoding as appropriate."),
  numberedItem("Feature scaling: continuous numeric attributes are standardised (zero mean, unit variance) so that distance-based algorithms such as k-Nearest Neighbours and Support Vector Machines are not dominated by attributes with naturally larger numeric ranges."),
  numberedItem("Class balancing: because academically \"at-risk\" students are typically a minority class relative to average and high-performing students, the Synthetic Minority Over-sampling Technique (SMOTE) (Chawla et al., 2002) is applied to the training partition only, generating synthetic minority-class examples by interpolating between existing minority-class neighbours, rather than by naive duplication. Applying SMOTE strictly to the training partition, after the train-test split, is essential to avoid information leakage that would otherwise produce artificially optimistic evaluation results — a methodological safeguard the literature does not always make explicit."),
  numberedItem("Train-test partitioning: the cleaned, encoded dataset is split into training (80%) and testing (20%) subsets using stratified sampling to preserve the relative proportions of each performance class in both partitions."),

  h2("3.6 Feature Engineering and Selection"),
  p("Beyond the raw attributes listed in Table 3.1, the design incorporates lightweight feature engineering, including the derivation of an attendance-trend indicator (change in attendance rate over the semester) and an academic-momentum indicator (change in GPA between the two most recent semesters), both of which have been found informative in related early-warning work (Akçapınar et al., 2019; Baneres et al., 2020). For feature selection, the design specifies two complementary techniques: a filter-based method (Information Gain, following Mamudu et al., 2026) to rank candidate features by their statistical association with the target class, and a wrapper-based method (Recursive Feature Elimination) to iteratively remove the least useful features based on a chosen model's performance. Where the two methods disagree substantially on a feature's relevance, that feature is retained for further inspection using SHAP values (Section 3.9) rather than discarded outright, since a feature with low univariate importance can still play a meaningful role in interaction with other features within tree-based ensembles."),

  h2("3.7 Machine Learning Algorithms Employed"),
  p("Six classification algorithms, selected to span a representative range of algorithmic families and interpretability profiles, and consistent with the algorithms most frequently compared in the reviewed literature (Ahmed, 2024; Mamudu et al., 2026), are specified for the proposed system:"),
  bulletItem("Logistic Regression: a linear, highly interpretable baseline classifier that models the log-odds of class membership as a linear combination of input features."),
  bulletItem("Decision Tree: a non-linear, inherently interpretable classifier that recursively partitions the feature space based on information-gain or Gini-impurity criteria."),
  bulletItem("k-Nearest Neighbours (KNN): an instance-based classifier that assigns a class based on the majority class among the k most similar training examples in feature space."),
  bulletItem("Naïve Bayes: a probabilistic classifier based on Bayes' theorem, assuming conditional independence between features given the class."),
  bulletItem("Support Vector Machine (SVM): a classifier that seeks the hyperplane (or, with a kernel function, a non-linear decision boundary) that maximally separates classes in feature space."),
  bulletItem("Random Forest (Breiman, 2001): an ensemble of decision trees trained on bootstrapped samples and random feature subsets, whose predictions are aggregated by majority vote; typically among the strongest-performing classical algorithms in the reviewed literature (Mamudu et al., 2026; Niazkar et al., 2024)."),
  p("Algorithm 3.1 summarises, in pseudocode, the overall training and evaluation procedure specified for the system."),
  h3("Algorithm 3.1: Model Training and Evaluation Procedure"),
  p("INPUT: cleaned dataset D with features X and target y", { }),
  p("1.  Split D into D_train (80%) and D_test (20%) using stratified sampling", { }),
  p("2.  Fit a scaler on D_train's numeric features; transform D_train and D_test", { }),
  p("3.  Apply SMOTE to D_train only, producing a class-balanced D_train_bal", { }),
  p("4.  Rank features on D_train_bal using Information Gain; shortlist top-k features", { }),
  p("5.  FOR each algorithm A in {LogReg, DecisionTree, KNN, NaiveBayes, SVM, RandomForest}:", { }),
  p("6.       Train A on D_train_bal using k-fold cross-validation for hyperparameter tuning", { }),
  p("7.       Evaluate trained A on D_test; record Accuracy, Precision, Recall, F1, AUC", { }),
  p("8.       Compute SHAP values for A on D_test to assess feature contribution", { }),
  p("9.  END FOR", { }),
  p("10. Select the model with the best F1-score (subject to acceptable fairness audit) as the deployment candidate", { }),
  p("OUTPUT: trained, evaluated models; comparative performance table; feature-importance report", { }),

  h2("3.8 Model Training and Testing Strategy"),
  p("Each algorithm is trained using stratified k-fold cross-validation (k = 5) on the training partition to tune hyperparameters (e.g., tree depth for Decision Tree and Random Forest; the number of neighbours, k, for KNN; the kernel and regularisation parameter, C, for SVM) while guarding against overfitting to any single train-validation split. Final performance is then reported on the held-out test partition, which is used only once, after all tuning is complete, to provide an unbiased estimate of generalisation performance. This separation between tuning and final evaluation data is a methodological safeguard not always made explicit in the reviewed literature and is treated as a required design feature here."),

  h2("3.9 Evaluation Metrics"),
  p("Given that student-performance classification is a multi-class problem with class imbalance, accuracy alone is an insufficient evaluation criterion, since a model could achieve high accuracy simply by always predicting the majority class. The proposed system therefore reports the following metrics, computed on the held-out test set:"),
  bulletItem("Accuracy: the proportion of all predictions that are correct."),
  bulletItem("Precision (macro-averaged): of the students predicted to belong to a given class, the proportion who actually belong to that class, averaged equally across classes."),
  bulletItem("Recall (macro-averaged): of the students who actually belong to a given class, the proportion correctly identified, averaged equally across classes — of particular importance for the At-Risk class, since a false negative here (failing to flag a genuinely at-risk student) is the most costly type of error for the system's intended purpose."),
  bulletItem("F1-score (macro-averaged): the harmonic mean of precision and recall, used as the primary criterion for model selection because it balances both concerns."),
  bulletItem("Area Under the Receiver Operating Characteristic Curve (AUC), computed using a one-vs-rest scheme for the multi-class setting, as an additional threshold-independent measure of discriminative ability."),
  bulletItem("Confusion matrix: a full breakdown of predicted versus actual class membership, used to diagnose specific patterns of misclassification (e.g., whether At-Risk students are more often confused with Average or High-Performing students)."),
  bulletItem("SHAP-based feature attribution: used not to evaluate raw predictive performance but to satisfy the interpretability requirement specified in Section 3.3.2."),

  h2("3.10 System Architecture"),
  p("Figure 3.1 presents the proposed layered system architecture, moving from raw institutional data sources through preprocessing, feature engineering, model training and evaluation, to a prediction and decision-support layer consumed by academic advisers and departmental staff."),
  ...figureImage("../simulation/fig_architecture.png", 430, 560, "Figure 3.1: Proposed system architecture for the ML-based student academic performance prediction system."),
  p("The architecture is deliberately modular: the preprocessing, feature-engineering, and modelling layers can be updated independently (for example, retraining models on a new cohort, or adding a new algorithm) without requiring changes to the data-ingestion or decision-support layers. This modularity also supports the fairness-auditing and interpretability requirements specified in Section 3.3.2, since the evaluation module is architecturally separated from, and sits between, model training and the decision-support layer that academic staff actually interact with — ensuring that no prediction reaches an adviser without having first passed through documented performance and fairness checks."),

  h2("3.11 Ethical Considerations"),
  p("The design explicitly incorporates the following safeguards, motivated directly by concerns raised in the literature reviewed in Chapter Two (Kizilcec & Lee, 2020; Mamudu et al., 2026):"),
  bulletItem("Human-in-the-loop decision-making: model outputs are framed strictly as decision-support signals for academic advisers, not as automated determinations of a student's standing; no adverse academic action is to be taken on the basis of a model prediction alone."),
  bulletItem("Exclusion of ethically sensitive proxy variables: as noted in Section 3.4, ethnicity, geopolitical zone of origin, and similar variables are excluded from the feature set, following the evidence from Adekitan and Salau (2020) that they add negligible genuine predictive value while carrying meaningful fairness risk."),
  bulletItem("Mandatory fairness auditing: before any deployment, the design requires the disaggregation of the evaluation metrics in Section 3.9 by gender, income level, and other relevant subgroups, following the audit approach demonstrated by Mamudu et al. (2026)."),
  bulletItem("Data minimisation and access control: only the attributes specified in Section 3.4 are to be collected and processed, and access to individually identifiable predictions is to be restricted to authorised academic advisers, consistent with the Nigeria Data Protection Act 2023."),
  bulletItem("Informed consent and transparency: students should be informed that such a system exists, what data it uses, and how its outputs may be used, consistent with general data-protection good practice."),
];

module.exports = { chapterThree };
